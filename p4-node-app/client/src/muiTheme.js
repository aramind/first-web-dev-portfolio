import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#15664F",
      light: "#3D816E",
      dark: "#1a5559",
    },
    hovercolor: {
      text: "#00e1ff",
    },
  },
});
